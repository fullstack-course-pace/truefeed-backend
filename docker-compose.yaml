version: "3.8"

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    # image name is provided via environment substitution in CI
    image: ${IMAGE_NAME:-truefeed-backend}:${IMAGE_TAG:-latest}
    restart: on-failure
    env_file:
      - ./.env